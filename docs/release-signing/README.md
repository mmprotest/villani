# Release signing placeholder

No signing key, certificate, token, or fabricated signature is stored in this repository. Local release candidates are marked `unsigned_release_candidate` and receive SHA-256 checksums. A future publication workflow must inject externally managed Windows code-signing, macOS Developer ID/notarization, and provenance signing credentials.
